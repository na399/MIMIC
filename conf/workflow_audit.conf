{
    "workflow": "audit",
    "comment": "Post-ETL audits (population + mapping rates)",
    "type": "sql",
    "scripts": [
        {"script": "etl/audit/post_etl_audit.sql", "comment": "populate audit tables and fail on empty required targets"}
    ]
}


{
    "workflow": "publish",
    "comment": "Create clean OMOP table names in a separate schema",
    "type": "sql",
    "scripts": [
        {"script": "etl/publish/publish_omop.sql", "comment": "publish omop views"}
    ]
}


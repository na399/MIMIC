{
    "workflow": "publish",
    "comment": "Materialize final OMOP tables (no views) and embed vocabulary",
    "type": "sql",
    "scripts": [
        {"script": "etl/publish/publish_omop.sql", "comment": "publish omop tables"}
    ]
}

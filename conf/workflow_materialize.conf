{
    "workflow": "materialize",
    "comment": "Optional: materialize published OMOP views into base tables",
    "type": "py",
    "scripts": [
        {
            "script": "scripts/materialize_omop_schema.py",
            "args": [
                "--database", "@duckdb_path",
                "--schema", "@omop_schema",
                "--enable", "@publish_materialize"
            ]
        }
    ]
}

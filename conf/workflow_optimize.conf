{
    "workflow": "optimize",
    "comment": "Optional DuckDB index creation for faster full runs",
    "type": "py",
    "scripts": [
        {
            "script": "scripts/optimize_duckdb_indexes.py",
            "args": [
                "--database", "@duckdb_path",
                "--schema", "@etl_dataset",
                "--enable", "@optimize_enable",
                "--enable-large", "@optimize_enable_large"
            ]
        }
    ]
}


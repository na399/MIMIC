{
    "workflow": "vocabulary_refresh",
    "type": "py",
    "scripts": [
        {
            "script": "scripts/load_vocab_to_duckdb.py",
            "args": [
                "--athena-export", "@athena_export_path",
                "--database", "@vocab_db_path",
                "--schema", "@vocab_schema"
            ]
        }
    ]
}
